// Item counts shown per section on the /community landing page — each section is a preview that
// links to its full page. The markdown twin (buildCommunityMarkdown) mirrors these so it cannot
// drift from the rendered page.
export const COMMUNITY_EVENTS_LIMIT = 5;
export const COMMUNITY_SHOWCASE_LIMIT = 8;
export const COMMUNITY_TOOLS_LIMIT = 3;
