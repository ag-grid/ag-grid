---
import styles from './ImageCaption.module.scss';

interface Props {
    className?: string;
}

const { className } = Astro.props as Props;
---

<div class:list={[styles.body, className]}>
    <div class={styles.bodyText}><slot /></div>
</div>
