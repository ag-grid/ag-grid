export { ClientSideRowModelModule } from './clientSideRowModelModule';

// TODO: we are temporarily exporting this module to allow the grid to use it
// this will be moved, but as Pete and Stephen pointed out, it does not make
// sense to create now a new module as modules will disappear.
export { TreeDataModule } from './treeData/treeDataModule';
