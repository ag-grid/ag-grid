{
  "name": "@ag-grid-community/styles",
  "version": "27.2.1",
  "description": "AG Grid Styles and Themes",
  "main": "_index.scss",
  "files": [
    "_index.scss",
    "dist/*"
  ],
  "scripts": {
    "build": "npm run build:sass -- --no-error-css && node build-css-content.js",
    "build:sass": "sass --no-source-map --load-path src/internal src:dist",
    "watch": "npm-run-all build --parallel watch:sass watch:content",
    "watch:content": "chokidar 'dist/*.css' --command 'node build-css-content.js'",
    "watch:sass": "npm run build:sass -- --watch",
    "$COMMENT": "TODO remove these tmp scripts once core uses this module",
    "tmp-watch-patch": "npm run tmp-build-patch; npm run tmp-build-patch -- --watch",
    "tmp-build-patch": "sass --no-source-map --load-path src/internal src:../core/dist/styles",
    "test": "npm run lint:scss && npm run build && npm run lint:css",
    "lint:css": "stylelint --formatter unix --config stylelint-config-css.js 'dist/*.css'",
    "lint:scss": "stylelint --formatter unix --config stylelint-config-scss.js 'src/**/*.scss' '*.scss'",
    "hash": "sh ../../scripts/hashDirectory.sh > .hash",
    "update-icon-fonts": "cd icon-fonts && npm i && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ag-grid/ag-grid.git"
  },
  "keywords": [
    "ag",
    "ag-grid",
    "style",
    "theme"
  ],
  "author": "Niall Crosby <niall.crosby@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ag-grid/ag-grid/issues"
  },
  "homepage": "http://www.ag-grid.com/",
  "devDependencies": {
    "@vusion/webfonts-generator": "^0.8.0",
    "chokidar-cli": "^3.0.0",
    "npm-run-all": "^4.1.5",
    "postcss-scss": "^4.0.4",
    "sass": "^1.51.0",
    "stylelint": "^14.8.2",
    "stylelint-csstree-validator": "^2.0.0",
    "stylelint-scss": "^4.2.0"
  }
}
