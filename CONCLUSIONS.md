1. **Agents almost never enable the grid's own validation** — 1 of 48 runs called
   `enableDevValidations()`

2. **Column sizing fails every time** —
   [`column-sizing`](tmp-llm-onboarding-evals/criteria/column-sizing/CRITERIA.md) failed 3 of 3 runs,
   reaching for a different wrong approach each time.

3. **Cell editing fails every time** —
   [`cell-editing`](tmp-llm-onboarding-evals/criteria/cell-editing/CRITERIA.md) failed 3 of 3 runs,
   mixing the grid-owned and app-owned editing models in every one.
