
// we use the grid options from the exampleInfiniteServerSide.js,
// and then override the following properties

gridOptions.pagination = true;
gridOptions.paginationPageSize = 1000;
gridOptions.infiniteBlockSize = 100;
