import type { _ModuleWithoutApi } from 'ag-grid-community';

import { VERSION } from '../version';

/**
 * @internal
 */
export const RichSelectAsyncRequestsModule: _ModuleWithoutApi = {
    moduleName: 'RichSelectAsyncRequests',
    version: VERSION,
};
