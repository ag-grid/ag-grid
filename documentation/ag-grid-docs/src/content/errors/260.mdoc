---
description: A component was configured on a grid whose module has not been registered.
---

## Module Not Registered

The component named in the message above needs a module in order to run, and that module was never registered. The message names the module to register, and shows the import and `registerModules` call for it.

{% partial file="./_modules-not-registered.mdoc" /%}
