---
description: A component was configured on a grid whose module has not been registered.
---

## Module Not Registered

The component named in the message above needs the missing module in order to run, and that module was never registered. Register {% errorParam name="moduleName" fallback="RowGrouping" suffix="Module" /%} to enable it.

{% partial file="./_modules-not-registered.mdoc" /%}
