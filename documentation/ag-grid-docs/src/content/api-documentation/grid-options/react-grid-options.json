{
    "_config_": {},
    "gridOptions": {
        "meta": {
            "displayName": "Grid Options"
        },
        "gridOptions": {
            "description": "All the above properties can also be specified on a single `gridOptions` object. If an option is set via `gridOptions`, as well as directly on the component, then the component value will take precedence. `gridOptions` is only read during initialisation, if you change it afterwards it will not impact the grid.",
            "type": "GridOptions",
            "tags": [{ "name": "initial" }]
        }
    },
    "reactRendering": {
        "meta": {
            "displayName": "React Rendering"
        },
        "renderingMode": {
            "description": "Fine grained control over the row rendering mechanism. <br/> `default` - recommended rendering approach. <br/> `legacy` - provided for backwards compatibility with AG Grid <= v33. Is susceptible to 'Maximum Update Depth Exceeded' errors and likely to be deprecated in a future version.",
            "default": "'default'",
            "type": "'default' | 'legacy'",
            "tags": [{ "name": "initial" }]
        }
    }
}
