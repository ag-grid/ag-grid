{% if isFramework("vue") %}

```js
this.gridOptions = {
    toolbar: {
        items: [
            {
                key: 'autoSizeAll',
                toolbarItem: 'customToolbarButton',
                toolbarItemParams: {
                    label: 'Auto Size All',
                    onClick: (api) => api.autoSizeAllColumns(),
                },
            },
            'quickFilter',
        ],
    },
    // ...other properties
}
```

{% /if %}
