{% if isFramework("angular") %}

Custom filter components implement the `IFilterDisplayAngularComp` interface. The `agInit(params)` method receives the filter params when the component is initialised.

Full details of the `IFilterDisplayAngularComp` interface are listed below under [API Reference](#api-reference).

{% /if %}
