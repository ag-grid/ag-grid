{% if isFramework("javascript") %}

Custom filter components implement the `FilterDisplayComp` interface. The `getGui()` method returns the filter's DOM element, and `refresh(params)` is called whenever the filter params change.

Full details of the `FilterDisplayComp` interface are listed below under [API Reference](#api-reference).

{% /if %}
