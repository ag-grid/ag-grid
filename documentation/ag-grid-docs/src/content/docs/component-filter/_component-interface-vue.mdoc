{% if isFramework("vue") %}

Custom filter components implement the `FilterDisplay` interface. When a Vue component is instantiated the grid makes the grid APIs, utility methods, and the filter params available via `this.params`.

Full details of the `FilterDisplay` interface are listed below under [API Reference](#api-reference).

{% /if %}
