// The random data is generated through a seeded random generator for predictable randomness for reproducibility. Do not use this in production.
// might generate sql queries from the Server-Side Row Model request.
// To keep things simple it patches the Math.random function to use the seeded random generator from 'seedrandom'.
Math.seedrandom('AG Grid Random Seed');
