import { Observable } from "../util/observable";
export declare class DropShadow extends Observable {
    enabled: boolean;
    color: string;
    xOffset: number;
    yOffset: number;
    blur: number;
}
