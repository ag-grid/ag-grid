This config is used when running the tests locally in development so that you can choose a subset of examples to run.

When run in CI, the full set of examples is downloaded to the `.cache` directory and used to run the tests.

See the `getFrameworkExamples` function in `exampleTestRunner.ts` for more details on how the examples are loaded.